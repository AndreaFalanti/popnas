{
    "training_hyperparameters": {
        "epochs": 200,
        "drop_path": 0.4,
        "softmax_dropout": 0.0,
        "optimizer": {
            "type": "adamW",
            "scheduler": "cd"
        },
        "label_smoothing": 0.1
    },
    "architecture_hyperparameters": {
        "multi_output": true
    },
    "dataset": {
        "folds": 1,
        "samples": null,
        "data_augmentation": {
            "enabled": true,
            "use_cutout": true
        }
    },
    "others": {
        "enable_XLA_compilation": true
    }
}